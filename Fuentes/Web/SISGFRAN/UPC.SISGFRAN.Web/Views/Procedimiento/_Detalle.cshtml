@model UPC.SISGFRAN.EL.Inherited.ReporteEvaluacionEL

<div>
    @{
        var grid = new WebGrid(
                    canPage: false,
                    canSort: false,
                    ajaxUpdateContainerId: "gridDetalle");
        grid.Bind(Model.EvaluacionDetalle, autoSortAndPage: false);
        @grid.GetHtml(htmlAttributes: new { id = "gridDetalle1" },
                    tableStyle: "webgrid",
                    headerStyle: "webgrid-header",
                    footerStyle: "webgrid-footer",
                    alternatingRowStyle: "webgrid-alternating-row",
                    selectedRowStyle: "webgrid-selected-row",
                    rowStyle: "webgrid-row-style",
                    fillEmptyRows: false,
                    mode: WebGridPagerModes.All,
                    columns: grid.Columns(
                        grid.Column("Procedimiento.Nombre", "Procedimiento"),
                        grid.Column("Procedimiento.Version", "Última versión"),
                        grid.Column("CantidadReclamos", "# reclamos"),
                        grid.Column("CantidadSugerencia", "# Sugerencias"),
                        grid.Column(header: "Calificación", format: @<text><img src="@item.Semaforo" id="adimg" /> @item.Calificacion </text>)
            ));
    }

</div>